% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_depth.R
\name{read_depth}
\alias{read_depth}
\title{Read the contig depth table}
\usage{
read_depth(path, long = TRUE)
}
\arguments{
\item{path}{Path to the depth file.}

\item{long}{If \code{TRUE} (the default), returns one row per contig and sample.
If \code{FALSE}, returns the table as written by the tool.}
}
\value{
A data frame. In long format it has the columns \code{contig},
\code{contig_length}, \code{sample}, \code{depth} and \code{depth_var}.
}
\description{
Reads the table written by \code{jgi_summarize_bam_contig_depths}
(\verb{03_mapping/<assembly>.depth.txt}), which holds the coverage of every
contig in every sample.
}
\examples{
path <- system.file("extdata", "example_run", "coassembly.depth.txt",
                    package = "magreport")
depth <- read_depth(path)
head(depth)
}
