% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_taxonomy.R
\name{plot_taxonomy}
\alias{plot_taxonomy}
\title{Plot how many MAGs were recovered per taxon}
\usage{
plot_taxonomy(mags, rank = "phylum", top = 15, fill_by = NULL)
}
\arguments{
\item{mags}{A data frame returned by \code{\link[=read_mag_quality]{read_mag_quality()}}.}

\item{rank}{Taxonomic rank: one of \code{"domain"}, \code{"phylum"}, \code{"class"},
\code{"order"}, \code{"family"}, \code{"genus"} or \code{"species"}.}

\item{top}{Maximum number of taxa to show. The rest are grouped into
"Other". Use \code{NULL} to show them all.}

\item{fill_by}{Optional column used to colour the bars, for example
\code{"quality"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Counts MAGs at the chosen taxonomic rank. MAGs that GTDB-Tk could not
classify at that rank are kept together in an "Unclassified" group, which is
usually the most informative part of the figure.
}
\examples{
path <- system.file("extdata", "example_run", "mag_quality.tsv",
                    package = "magreport")
mags <- read_mag_quality(path)
plot_taxonomy(mags, rank = "phylum")
plot_taxonomy(mags, rank = "genus", fill_by = "quality")
}
