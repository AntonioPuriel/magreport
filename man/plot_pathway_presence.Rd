% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pathway_presence.R
\name{plot_pathway_presence}
\alias{plot_pathway_presence}
\title{Plot which MAGs carry the genes of each process}
\usage{
plot_pathway_presence(
  kos,
  markers = hydrocarbon_kos(),
  mags = NULL,
  rank = "genus",
  min_kos = 1
)
}
\arguments{
\item{kos}{A data frame returned by \code{\link[=read_ko_table]{read_ko_table()}}.}

\item{markers}{A data frame with the columns \code{pathway} and \code{ko}. Defaults to
\code{\link[=hydrocarbon_kos]{hydrocarbon_kos()}}.}

\item{mags}{Optional data frame returned by \code{\link[=read_mag_quality]{read_mag_quality()}}, used to
label each MAG with its taxonomy instead of its bin name.}

\item{rank}{Taxonomic rank used for the labels when \code{mags} is given.}

\item{min_kos}{Minimum number of marker genes a MAG must carry to be shown.
Use \code{0} to keep every MAG.}
}
\value{
A \code{ggplot} object. The fill is the number of marker genes of that
process found in the MAG.
}
\description{
Crosses the KEGG orthologues found in every MAG with a set of marker genes
and draws a heatmap of MAGs against processes. It answers the question that
usually follows MAG recovery in a polluted site: which genomes can do what.
}
\examples{
kos <- read_ko_table(system.file("extdata", "example_run", "ko_per_mag.tsv",
                                 package = "magreport"))
plot_pathway_presence(kos)
}
