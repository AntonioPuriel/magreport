% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mapping_rates.R
\name{plot_mapping_rates}
\alias{plot_mapping_rates}
\title{Plot the alignment rate of each sample}
\usage{
plot_mapping_rates(mapping, warn_below = 70)
}
\arguments{
\item{mapping}{A data frame returned by \code{\link[=read_mapping_summary]{read_mapping_summary()}}.}

\item{warn_below}{Alignment rate under which bars are highlighted, in
percent. Use \code{NULL} to disable the highlight.}
}
\value{
A \code{ggplot} object.
}
\description{
A low alignment rate means that a large part of a sample is not represented
in the assembly, which is worth checking before interpreting coverage. When
samples were mapped against several assemblies (mag-pipeline with
\verb{--assembly_mode group} or \code{per_sample}), each assembly gets its own panel.
}
\examples{
path <- system.file("extdata", "example_run", "mapping_summary.tsv",
                    package = "magreport")
mapping <- read_mapping_summary(path)
plot_mapping_rates(mapping)
}
